{
  "name": "gc-website",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate:pwa-icons": "node scripts/generate-pwa-icons.mjs",
    "firebase:login": "npm exec --package firebase-tools -- firebase login --no-localhost",
    "firebase:deploy-rules": "npm exec --package firebase-tools -- firebase deploy --only firestore:rules --project the-glorious-church",
    "firebase:deploy-functions": "npm exec --package firebase-tools -- firebase deploy --only functions --project the-glorious-church",
    "firebase:emulators:start": "npm exec --package firebase-tools -- firebase emulators:start --only firestore,functions"
  },
  "dependencies": {
    "firebase": "^10.14.1",
    "leaflet": "^1.9.4",
    "lucide-react": "^0.469.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-leaflet": "^4.2.1",
    "react-router-dom": "^6.28.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "sharp": "^0.35.3",
    "tailwindcss": "^4.0.0",
    "vite": "^6.0.3",
    "vite-plugin-pwa": "^1.3.0"
  }
}
